# Headless tooling

This directory contains Node-only command-line and test helpers for local automation. It is not loaded by Obsidian as part of the plugin runtime. The published Community Plugin runtime assets are `main.js`, `manifest.json`, and `styles.css`.
